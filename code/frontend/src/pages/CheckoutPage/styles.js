import {createUseStyles} from "react-jss";

export const useCheckoutPageStyles = createUseStyles({
    tagCls: {
        padding: ".5em",
        fontSize: "2em"
    },
    fullWidthCls: {
        width: "100%"
    }
});
