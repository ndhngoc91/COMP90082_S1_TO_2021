import {createUseStyles} from "react-jss";

export const usePackagePageStyles = createUseStyles({
    packageItemCls: {
        background: "#fff",
        padding: "1em",
        boxShadow: "rgba(99, 99, 99, 0.2) 0px 2px 8px 0px"
    }
});
