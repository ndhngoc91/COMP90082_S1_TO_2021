import {createUseStyles} from "react-jss";

export const useNavigationBarStyles = createUseStyles({
    leftItemCls: {
        float: "left",
    },
    rightItemCls: {
        float: "right"
    }
});
