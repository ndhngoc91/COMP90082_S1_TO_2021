import {createUseStyles} from "react-jss";

export const useNavigationBarStyles = createUseStyles({
    logoCls: {
        height: "100%"
    },
    leftItemCls: {
        float: "left",
    },
    rightItemCls: {
        float: "right"
    }
});
